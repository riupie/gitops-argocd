yamlApplicationConfig:
  kafka:
    clusters:
      - name: kafka-demo
        bootstrapServers:  kafka-demo-kafka-bootstrap.kafka.svc.cluster.local:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false